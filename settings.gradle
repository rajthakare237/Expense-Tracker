pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()        // To implement pdfViewing library
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Expense Tracker"
include ':app'
